FROM node:24-alpine

WORKDIR /app

COPY package*.json ./

RUN npm install

EXPOSE 5173

# Source code is mounted as a volume — no COPY . . needed.
# Vite dev server with --host 0.0.0.0 exposes HMR to the Docker network.
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
