FROM node:24-alpine

WORKDIR /app

COPY package*.json ./

RUN npm ci

# Copy root-level config files needed by Vite at runtime
COPY index.html vite.config.ts tsconfig*.json ./

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
